hl.config({
  input = {
    kb_options = "caps:none",
    repeat_rate = 40,
    repeat_delay = 250,
    numlock_by_default = true,

    touchpad = {
      clickfinger_behavior = true,
      scroll_factor = 0.4,
      disable_while_typing = false,
    },

    tablet = {
      relative_input = true,
    },
  },
})
